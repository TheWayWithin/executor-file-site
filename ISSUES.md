# executor-file-site — Issue & Project Register

**This is the single source of truth for what is open in this repo.** One row per
issue/project. Detail lives in the linked doc; this file is the index the Mission
Control reconcile (`repo-reconcile.py`) reads and mirrors to the cockpit.


**Close format (machine contract):** a resolved row's Status cell must contain
`✅ Resolved YYYY-MM-DD — note` (the literal `Resolved YYYY-MM-DD` is what
mc-shipped/mc-throughput parse; `Done` without a date is invisible to metrics).

## ID convention (collision-safe)

Mission Control owns the bare `ISS-`/`PRJ-`/`T-` namespaces. **Every executor-file-site ID
carries the `EFS-` prefix** so it can never collide with a Mission-Control-native
ID or another repo's. Raise issues here with `python3 ~/shared/scripts/repo-issue.py`.

---

## Open

| ID | Title | Status | Severity | Detail | MC-SYNC |
|----|-------|--------|----------|--------|---------|
| EFS-ISS-7 | Tidy: src/mirrored/quickstart.md is still generated by fetch-mirror.mjs but no longer imported (get-started now uses site-native steps). Drop it from the fetch script on a future pass. | ✅ Resolved 2026-07-23 — Dropped quickstart.md from fetch-mirror.mjs outputs and deleted the orphan; grep shows zero remaining references; build passes clean (19 pages). | low | — | pending |

## Recently closed

| ID | Title | Status | Commit | Detail |
|----|-------|--------|--------|--------|
| EFS-ISS-9 | get-started editor step is inconsistent with the other steps: it leads with prose 'double-click Edit-Executor-File.command' (no command block) and buries scripts/edit.sh in a collapsed details, so users following the 'paste this command' rhythm miss it (real UAT: Jamie ran doctor.sh instead). Compounded by macOS quarantine blocking the double-clicked .command on downloaded releases. Fix: make scripts/edit.sh the primary, consistent command block; demote double-click to an optional note. | ✅ Resolved 2026-07-19 | get-started fix | scripts/edit.sh now the primary command block on the register step, consistent with the other steps; double-click demoted to an optional note that explains the macOS block. |
| EFS-ISS-8 | Learn posts for two high-intent queries not yet written: 'What happens to my online accounts when I die?' and 'How will my family find my passwords?' (now answered via FAQ questions; dedicated articles remain the highest-value content gap per agent-readability brief section 4) | ✅ Resolved 2026-07-19 | 3 learn articles | find-every-account, digital-legacy-graveyard, password-managers-keys-map published live 2026-07-19; covers the 'find accounts' and 'find passwords' queries. A dedicated 'what happens when I die' piece can be the T-129 article. |
| EFS-ISS-6 | get-started has no bridge from webpage to working terminal | ✅ Resolved 2026-07-18 — One continuous eight-step ordered list runs download -> unpack -> open Terminal and cd into the folder -> install -> register -> seal -> print -> fire drill, with no stranded instruction. | 3f6bf39 | One continuous eight-step ordered list runs download -> unpack -> open Terminal and cd into the folder -> install -> register -> seal -> print -> fire drill, with no stranded instruction. |
| EFS-ISS-5 | get-started steps render as bold prose not a real list | ✅ Resolved 2026-07-18 — Setup is now a real `<ol>` numbered from 1 (styled step badges), not mirrored bold prose. | 3f6bf39 | Setup is now a real `<ol>` numbered from 1 (styled step badges), not mirrored bold prose. |
| EFS-ISS-4 | get-started duplicates the install step in clashing tones | ✅ Resolved 2026-07-18 — Stopped mirroring the developer README quickstart verbatim; install age+ssss now appears once, in the site's own voice. | 3f6bf39 | Stopped mirroring the developer README quickstart verbatim; install age+ssss now appears once, in the site's own voice. |
| EFS-ISS-3 | get-started commands need unmistakable styling + placement | ✅ Resolved 2026-07-18 — Every command sits in a bordered block with a ">_ In the Terminal, paste this and press return" caption above the monospace line. | 3f6bf39 | Every command sits in a bordered block with a ">_ In the Terminal, paste this and press return" caption above the monospace line. |
| EFS-ISS-2 | get-started install step assumes terminal literacy | ✅ Resolved 2026-07-18 — The Terminal is explained in plain words (what it is, how to open it) once, in the step before the first typed command. | 3f6bf39 | The Terminal is explained in plain words (what it is, how to open it) once, in the step before the first typed command. |
| EFS-ISS-1 | get-started platform blocks don't signal 'pick one' | ✅ Resolved 2026-07-18 — Mac is the primary path shown inline; Linux and Windows sit behind clearly secondary "instead" toggles. | 3f6bf39 | Mac is the primary path shown inline; Linux and Windows sit behind clearly secondary "instead" toggles. |
